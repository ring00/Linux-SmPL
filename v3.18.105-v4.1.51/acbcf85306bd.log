--- /home/oslab/Desktop/common/net/netfilter/xt_qtaguid.c
+++ /tmp/cocci-output-17977-14dcee-xt_qtaguid.c
@@ -767,7 +767,8 @@ static int iface_stat_fmt_proc_show(stru
 	} else {
 		pp_iface_stat_line(m, iface_entry);
 	}
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 static const struct file_operations read_u64_fops = {
@@ -1489,7 +1490,8 @@ static int proc_iface_stat_fmt_open(stru
 		return -ENOMEM;
 
 	s->fmt = (uintptr_t)PDE_DATA(inode);
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 static const struct file_operations proc_iface_stat_fmt_fops = {
@@ -1554,7 +1556,8 @@ static int __init iface_stat_init(struct
 		       "failed to register ipv6 dev event handler\n");
 		goto err_unreg_ip4_addr;
 	}
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 
 err_unreg_ip4_addr:
 	unregister_inetaddr_notifier(&iface_inetaddr_notifier_blk);
@@ -1951,7 +1954,8 @@ static int qtaguid_ctrl_proc_show(struct
 		prdebug_full_state_locked(0, "proc ctrl");
 	}
 
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 /*
@@ -2323,7 +2327,8 @@ static int ctrl_cmd_tag(const char *inpu
 		 input, sock_tag_entry,
 		 atomic_read(&el_socket->sk->sk_refcnt));
 	sockfd_put(el_socket);
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 
 err_put:
 	CT_DEBUG("qtaguid: ctrl_tag(%s): done. ...->sk_refcnt=%d\n",
@@ -2430,7 +2435,8 @@ int qtaguid_untag(struct socket *el_sock
 
 	atomic64_inc(&qtu_events.sockets_untagged);
 
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 static ssize_t qtaguid_ctrl_parse(const char *input, size_t count)
@@ -2529,8 +2535,8 @@ static int pp_stats_line(struct seq_file
 			 get_atag_from_tag(tag), stat_uid,
 			 current->pid, current->tgid, from_kuid(&init_user_ns, current_fsuid()),
 			 from_kgid(&init_user_ns,xt_qtaguid_stats_file->gid));
-		return xt_check_entry_offsets(e, e->target_offset,
-					      e->next_offset);
+		return xt_compat_check_entry_offsets(e, e->target_offset,
+						     e->next_offset);
 	}
 	ppi->item_index++;
 	cnts = &ts_entry->counters;
@@ -2715,7 +2721,8 @@ static int qtaguid_stats_proc_show(struc
 	else
 		pp_sets(m, ts_entry);
 
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 /*------------------------------------------*/
@@ -2728,8 +2735,8 @@ static int qtudev_open(struct inode *ino
 	bool utd_entry_found;
 
 	if (unlikely(qtu_proc_handling_passive))
-		return xt_check_entry_offsets(e, e->target_offset,
-					      e->next_offset);
+		return xt_compat_check_entry_offsets(e, e->target_offset,
+						     e->next_offset);
 
 	DR_DEBUG("qtaguid: qtudev_open(): pid=%u tgid=%u uid=%u\n",
 		 current->pid, current->tgid, from_kuid(&init_user_ns, current_fsuid()));
@@ -2775,7 +2782,8 @@ static int qtudev_open(struct inode *ino
 	DR_DEBUG("qtaguid: tracking data for uid=%u in pqd=%p\n",
 		 from_kuid(&init_user_ns, current_fsuid()), new_pqd_entry);
 	file->private_data = new_pqd_entry;
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 
 err_unlock_free_utd:
 	if (!utd_entry_found) {
@@ -2796,8 +2804,8 @@ static int qtudev_release(struct inode *
 	struct tag_ref *tr;
 
 	if (unlikely(qtu_proc_handling_passive))
-		return xt_check_entry_offsets(e, e->target_offset,
-					      e->next_offset);
+		return xt_compat_check_entry_offsets(e, e->target_offset,
+						     e->next_offset);
 
 	/*
 	 * Do not trust the current->pid, it might just be a kworker cleaning
@@ -2865,7 +2873,8 @@ static int qtudev_release(struct inode *
 	prdebug_full_state_locked(0, "%s(): pid=%u tgid=%u", __func__,
 			   current->pid, current->tgid);
 	spin_unlock_bh(&sock_tag_list_lock);
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 /*------------------------------------------*/
@@ -2959,7 +2968,8 @@ static int __init qtaguid_proc_register(
 	 * TODO: add support counter hacking
 	 * xt_qtaguid_stats_file->write_proc = qtaguid_stats_proc_write;
 	 */
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 
 no_stats_entry:
 	remove_proc_entry("ctrl", *res_procdir);
@@ -2989,7 +2999,8 @@ static int __init qtaguid_mt_init(void)
 	    || xt_register_match(&qtaguid_mt_reg)
 	    || misc_register(&qtu_device))
 		return -1;
-	return xt_check_entry_offsets(e, e->target_offset, e->next_offset);
+	return xt_compat_check_entry_offsets(e, e->target_offset,
+					     e->next_offset);
 }
 
 /*
